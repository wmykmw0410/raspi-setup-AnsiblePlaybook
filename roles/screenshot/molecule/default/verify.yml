---
- name: Verify
  hosts: all
  become: true
  tasks:
    - name: gnome-screenshot がインストールされていることを確認する
      ansible.builtin.command: dpkg -s gnome-screenshot
      register: screenshot_pkg_status
      changed_when: false
      failed_when: screenshot_pkg_status.rc != 0
