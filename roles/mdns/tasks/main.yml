---
- name: avahi-daemon をインストールする
  ansible.builtin.apt:
    name: avahi-daemon
    state: present

- name: avahi-daemon を有効化して起動する
  ansible.builtin.service:
    name: avahi-daemon
    enabled: true
    state: started
