---
- name: Verify
  hosts: all
  become: true
  tasks:
    - name: swimmy が sudo グループに属していることを確認する
      ansible.builtin.command: id -nG swimmy
      register: base_groups_result
      changed_when: false
      failed_when: "'sudo' not in base_groups_result.stdout"

    - name: cups-browsed が無効化されていることを確認する
      ansible.builtin.command: systemctl is-enabled cups-browsed
      register: base_cups_status
      changed_when: false
      failed_when: "'masked' not in base_cups_status.stdout"
